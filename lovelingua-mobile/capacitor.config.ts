import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lovelingua.app',
  appName: 'LoveLingua',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
